---
sidebar_position: 4
title: 실전 적용 가이드
description: 15개 유스케이스의 현실 점검 — 즉시 가치를 얻는 곳과 추가 설정이 필요한 곳
---

# 실전 적용 가이드

> 15개 유스케이스의 **솔직한 현실 점검**. 어디서 즉시 가치를 얻고, 어디에 추가 설정이 필요한지.

---

## 즉시 활용 가능한 사례

입력이 **코드/설정 파일/텍스트**이고, 출력이 **분석/코드/문서**인 작업은 지금 바로 실용적입니다.

| UC | 시나리오 | 모드 | 왜 바로 되는가 |
|----|---------|------|--------------|
| UC1 | IAM 정책 검토 | `@verify` | JSON 텍스트 분석 → 교차검증 즉시 작동 |
| UC3 | Terraform 리팩토링 | `@build` | 파일 기반 설계→구현. 외부 의존성 없음 |
| UC5 | 오토스케일링 튜닝 | `@craft` | 설정 파일 분석 + 수정안 제시 |
| UC9 | DR 런북 작성 | `@design` | Terraform/CloudFormation 읽고 설계 문서 생성 |
| UC11 | API 마이그레이션 | `@build` | 설계→구현 파이프라인. 코드 생성이 핵심 |
| UC12 | 테스트 일괄 생성 | `@craft` | 소스 분석 → 테스트 코드 생성. **가장 실용적** |
| UC13 | PR 코드 리뷰 | `@verify` | git diff 텍스트 기반. 즉시 가능 |
| UC14 | 마이크로서비스 설계 | `@design` | 코드 분석 → 설계 문서. Kiro 핵심 강점 |

:::tip 공통점
이 8개 사례의 공통점은 **로컬 파일/텍스트만으로 완결**된다는 것입니다. 외부 API 호출이나 실시간 데이터가 불필요합니다.
:::

---

## AWS 연동이 필요한 사례

**실시간 AWS API 호출**, **외부 시스템 접근**이 전제되는 작업은 추가 설정이 필요합니다.

| UC | 시나리오 | 모드 | 제약 사항 |
|----|---------|------|----------|
| UC2 | CloudWatch 로그 분석 | `@scan` | Gemini CLI의 AWS API 호출에 네트워크/인증 설정 필요 |
| UC4 | CI/CD 장애 대응 | `@mobilize` | GitHub Actions + Terraform state 교차 확인 — Master 단독이 더 빠를 수 있음 |
| UC6 | P1 장애 대응 | `@mobilize` | 실시간 AWS 메트릭 조회 필요. Slave 타임아웃 제약 |
| UC8 | 비용 분석 | `@scan` | Cost Explorer API 호출 필요. Codex sandbox 네트워크 제한 |
| UC10 | 보안 감사 | `@mobilize` | 대량 AWS API 호출 — aws cli 스크립트가 더 현실적일 수 있음 |
| UC15 | DB 마이그레이션 | `@mobilize` | Pre-check 스크립트는 유용하지만 실행은 DBA가 직접 수행 |

:::info 제약이 있다고 가치가 없는 것은 아닙니다
UC6(P1 장애 대응)의 5-Layer 트리아지 프레임워크는 에이전트 없이 사람이 사용해도 유용합니다. UC15(DB 마이그레이션)의 전략 비교와 Pre-check 스크립트는 DBA에게 전달하는 체크리스트로 가치가 있습니다.
:::

---

## Multi-Agent가 단독보다 확실히 나은 경우

### 1. 교차검증 (`@verify`)

> IAM 리뷰, 코드 리뷰에서 **"두 번째 눈"의 가치는 실재**합니다.

- Gemini가 놓치는 **인증 누락**을 Codex가 잡음
- Codex가 놓치는 **컨벤션 위반**을 Gemini가 잡음
- Master가 두 결과를 **병합**하여 최적 버전 생성

### 2. 설계→구현 파이프라인 (`@build`)

> **"설계한 것과 구현한 것이 일치하는가?"** 자체 검증이 가능합니다.

- 단독 에이전트는 자기가 빠뜨린 것을 자기가 발견하기 어려움
- Kiro가 5개 설계했는데 Codex가 3개만 구현 → Master가 갭 식별
- 스펙 충족률이라는 **객관적 지표**로 품질 판단

### 3. 대량 코드 생성/분석 (`@craft`)

> 테스트 생성, 리팩토링처럼 **반복적이지만 정확해야 하는** 작업

- 비즈니스 로직을 이해한 의미 있는 테스트 생성
- `assert result is not None` vs `assert_called_once_with("p-1", 2)` 차이
- 47개 함수에 156개 테스트 → 커버리지 23% → 84%

---

## Master 단독이 더 효율적인 경우

### 1. AWS 실시간 조회

Claude가 MCP/도구로 직접 호출하는 게 Slave 위임보다 빠릅니다. Slave에게 프롬프트를 전달하고 결과를 기다리는 오버헤드가 불필요합니다.

### 2. 장애 대응 초기 5분

Slave 타임아웃을 기다리는 동안 상황이 악화될 수 있습니다. **Master가 먼저 빠르게 파악 후, 심층 분석만 Slave에 위임**하는 것이 현실적입니다.

### 3. 컨텍스트가 풍부한 작업

대화 맥락, 이전 작업 이력 등 Slave에게 전달하기 어려운 배경 지식이 많은 경우, Master 단독이 더 정확합니다.

### 4. 단순 질문/수정

3-Agent 오케스트레이션 오버헤드가 답변 지연만 유발합니다. `@ask`로 충분합니다.

---

## 시스템의 본질적 가치

유스케이스 카탈로그의 **진짜 가치**는 특정 사례의 실행 여부가 아니라, 세 가지 구조적 이점에 있습니다.

| 가치 | 설명 | 에이전트 없이도 적용 가능? |
|------|------|------------------------|
| **사고 프레임워크** | "이 상황에서 뭘 먼저 해야 하지?"에 대한 구조화된 접근법 | ✅ 모드 선택 가이드만으로 의사결정 품질 향상 |
| **4-Block Format** | 결론→근거→리스크→실행안 형식이 의사결정을 강제 구조화 | ✅ 포스트모템, 설계 리뷰에 즉시 적용 가능 |
| **자동 승격 규칙** | "이건 더 신중하게 해야 해"를 시스템이 판단 | ✅ 체크리스트로 사용하면 사람의 판단도 개선 |

:::note 핵심 인사이트
15개 사례 중 **8개는 지금 바로 실용적**이고, **7개는 AWS 연동 설정이 전제**됩니다. 하지만 가장 큰 가치는 개별 사례의 실행이 아니라, **"이 작업은 어떤 수준의 신중함이 필요한가?"라는 판단 체계** 자체입니다. 이것은 에이전트 없이도 팀의 의사결정을 개선합니다.
:::

---

## 유스케이스별 에이전트 활용 요약

| UC | 모드 | Kiro | Codex | Gemini | Master |
|----|------|------|-------|--------|--------|
| 1 | verify | - | 정밀 검증 | 패턴 매칭 | 병합 판정 |
| 2 | scan | - | - | 대량 분석 | 검증 |
| 3 | build | 모듈 설계 | 코드 구현 | - | 갭 분석 |
| 4 | mobilize | 트리아지 | 근본 원인 | 영향도 | Go/No-Go |
| 5 | craft | - | 메트릭 분석 | - | 검증 |
| 6 | mobilize | 트리아지 | 코드 추적 | 영향도 | Go/No-Go |
| 7 | verify | - | 수학 검증 | 프레임워크 | 병합 |
| 8 | scan | - | - | API 대량 호출 | 검증 |
| 9 | design | 런북 설계 | - | - | 검증 |
| 10 | mobilize | 구조화 | 정밀 점검 | 대량 조회 | 종합 판정 |
| 11 | build | 마이그레이션 설계 | 프록시 구현 | - | 갭 분석 |
| 12 | craft | - | 테스트 생성 | - | 검증 |
| 13 | verify | - | 로직 분석 | 패턴 매칭 | 병합 판정 |
| 14 | design | 서비스 설계 | - | - | 검증 |
| 15 | mobilize | 전략 설계 | 스크립트 검증 | 영향 분석 | Go/No-Go |
